package com.odenzo.accounting.frontend
import munit.FunSuite

/**
 * Well, what happens here. This is a Javascript only thing, but I will test wiith MUnit. Oh, munit needs junit so.... can only test stuff
 * with JVM libs. The shared code stuff.
 */
class GlobalUtilsTest extends FunSuite:

  test("nzDate") {

    def functionX: String =
      ("REL ID")

  }
